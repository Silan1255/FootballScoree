<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.footballscore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FootballScore">
        <activity android:name=".aaaaaaa"></activity>
        <activity
            android:name=".pages.TahminMenu"
            android:label="@string/title_activity_tahmin_menu" />
        <activity android:name=".pages.KayitOl" />
        <activity android:name=".pages.GirisEkrani" />
        <activity android:name=".pages.SplashSceen" />
        <activity android:name=".pages.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>