<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.footballscore">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FootballScore"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".view.TahminMenu"
            android:label="@string/title_activity_tahmin_menu" />
        <activity android:name=".view.MainActivity" />
        <activity android:name=".pages.LoginScreen" />
        <activity android:name=".view.KayitOl" />
        <activity android:name=".view.SplashSceen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
